# Only the scorer needs Python. The 6 steps are prompt-driven (run via GitHub
# Copilot / an LLM against your model through the Power BI Modeling MCP).
# Standard library only — no external packages required to run the scorer.
